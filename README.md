A simple application that allows the registered user to add personal notes and view and filter them.

# Installation

Initialize:<br>
python3 manage.py makemigrations<br>
python3 manage.py migrate

python3 manage.py runserver
